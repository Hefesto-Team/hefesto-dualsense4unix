# CONFERIDO-EM — a segunda porta do portão FOTOS-DA-VERSAO-01.
#
# A régua é `tests/unit/test_as_fotos_acompanham_a_versao.py`, e a primeira
# porta dela é a topologia: as fotos desta pasta têm de vir DEPOIS da última
# mexida em `src/…/interface`, `src/…/app` ou `src/…/gui` — a tupla
# `CODIGO_DA_TELA` da régua. Ela citava `scripts/gui-captura` até 06/09/2026,
# quando a pasta saiu com a janela GTK; o retratista de hoje mora dentro da
# primeira entrada, e é por isso que a tupla encolheu sem perder alcance.
#
# Só que a primeira porta pede um COMMIT de foto como prova de um GESTO. Quando
# o gesto não produz bytes — mudou-se código de tela e nenhuma imagem mudou —
# não há o que commitar, e o portão fica vermelho para sempre. Este arquivo é a
# saída, e ela não afrouxa nada:
#
#     rodou o retratista e as imagens MUDARAM   -> commite as imagens
#     rodou o retratista e nada mudou           -> declare aqui o commit
#
# Uma linha por conferência. O primeiro campo é o SHA COMPLETO (40) do commit de
# tela conferido; o resto da linha é para quem for auditar. `#` é comentário.
# A declaração envelhece sozinha: qualquer mexida nova na tela gera outro SHA,
# nenhuma linha daqui o nomeia, e o portão reabre.
#
# NÃO REFOTOGRAFE SÓ PARA GERAR BYTES. A foto carrega o estado VIVO da máquina
# de quem a tira — a linha "Controles detectados" e a cor do plástico lida de
# cada controle. Commitar a sua troca a mesa dela pela sua, calada.

2899ef0d97497b9eb3b353cd8515d61d4812c344  03/09/2026  retratista rodado com o `fatos_do_mapa.py` de antes e de depois deste commit, mesma máquina e mesma mesa: as 14 fotos byte a byte IDÊNTICAS. O commit mexeu em `app/fatos_do_mapa.py` (a tabela que a aba Emulação e a seção Controles leem), e moveu ZERO pixel.

HEAD  03/09/2026  retratista rodado com a interface de hoje. TRÊS fotos mudaram, e as três foram olhadas antes de decidir. As DUAS de Configurações entram no commit: elas mostram uma CURA — o Jogador 2 e o Jogador 3, os dois por Bluetooth, traziam uma caixa vazia dizendo "Escreva a cor" e agora dizem "Nova Pink (lido)" e "Midnight Black (lido)", com a bolinha da cor. É o `secao_controles.py` que parou de recusar a pergunta a quem está no rádio (`transporte != "usb": continue`). A aba usa uma mesa de fixture de quatro jogadores, então nada da máquina viva vaza nela. A de Emulação NÃO entra: a única diferença é a linha "Controles detectados", que dizia "2 controles físicos, 2 gamepads virtuais — 8 nós" e passaria a dizer "1 controle físico — 2 nós". Isso é a mesa de agora, não o produto — commitá-la trocaria a mesa dela pela de quem rodou, calada, que é exatamente o que esta régua avisa.

901e6b066f21a47447de1ab2697ccb9952d393ce  04/09/2026  retratista rodado na mesma máquina. DUAS fotos mudaram e as duas foram OLHADAS antes de decidir. A de Navegação ENTRA: a mudança é de CONTEÚDO — a linha do L3 dizia "Abrir teclado na tela" e passou a dizer "Abrir e fechar teclado na tela", que é o que o botão faz. A de Emulação NÃO entra, pelo mesmo motivo da declaração acima: a única diferença é "Controles detectados", de "2 controles físicos, 2 gamepads virtuais — 8 nós" para "1 controle físico, 1 gamepad virtual — 3 nós". Isso é a mesa DE AGORA, não o produto. Medido por GdkPixbuf: a de Navegação difere em 11 linhas de 1080 (y 553-563), e nenhuma outra foto moveu um pixel.

d5966b66018bfd2520c2b77222e325bc817b2368  04/09/2026  retratista rodado na árvore de integração da leva do PO (ONDA 0 + ONDA 1: a folha, o piloto, o som e os fatos). Das 14 fotos, TREZE saíram byte a byte idênticas e UMA mudou, e ela foi OLHADA antes de decidir — recorte lado a lado, medido com `compare -metric AE`: 4.229 pixels, todos dentro de uma caixa de 511x36 em +227+172. A de Emulação NÃO entra, pela terceira vez e pelo mesmo motivo das duas declarações acima: a única diferença é a linha "Controles detectados", que dizia "2 controles físicos, 2 gamepads virtuais — 8 nós em /dev/input/js*" e passaria a dizer "1 controle físico, 1 gamepad virtual — 4 nós". É a mesa DE AGORA, não o produto. O que a leva mexeu de tela — `app/theme.py` (o lado dos botões da janela), `app/ipc_bridge.py` e `app/audio_saida.py` — moveu ZERO pixel nas onze abas da janela antiga, e é isso que estas treze fotos idênticas provam.

24fd096e0dbd46262ec07f34b97f9d0d3913cfb3  04/09/2026  retratista rodado na árvore da integração de 04/09, já com as trocas de comentário desta frente aplicadas. Das 14 fotos, TREZE saíram byte a byte idênticas (sha256) e UMA mudou: a de Emulação, pela QUARTA vez e pelo mesmo motivo das três declarações acima — a única diferença é a linha "Controles detectados", que na foto commitada diz "2 controles físicos, 2 gamepads virtuais — 8 nós" e hoje diria "1 controle físico, 1 gamepad virtual — 4 nós". É a mesa DE AGORA, não o produto. Medido pixel a pixel com GdkPixbuf: 4.229 pixels em 28 linhas, todos dentro de y 172-207 × x 227-737 — a MESMA caixa de 511x36 em +227+172 da declaração de d5966b66. O que a leva mexeu de tela desde 83577f9d — `gui/ponte_da_tela.py` (o `self.morreu` da ponte WebKit), `app/ipc_bridge.py` e `app/telas/vibracao.py` (a barra por motor, que a janela antiga não desenha) — moveu ZERO pixel nas onze abas, e é isso que as treze fotos idênticas provam.

8b0d5fc281263bc78a72fac5f5862a1032a3c92b  05/09/2026  PRIMEIRA declaração depois de `src/hefesto_dualsense4unix/interface` entrar em `CODIGO_DA_TELA` — e ela existe por causa dessa entrada. As DEZ fotos da interface nova (`aba-NN-*.png`) foram refeitas com `interface/olhar.py --todas --publicado --doc` nesta árvore e saíram BYTE A BYTE idênticas às commitadas (sha256 das dez, comparado antes e depois). O que a leva mexeu em `interface/` desde 3b6642ce foi PYTHON e PROSA — o piloto (a piscada da 03-Q4, o pouso com desfecho, o `FRASE_DE_SUCESSO` morto), o retratista `olhar.py`, o `canal_do_microfone.py` novo e dez citações de linha em comentário. Nenhuma das dez páginas de `interface/paginas/` foi tocada, e é isso que as dez fotos idênticas provam. As onze `readme_*.png` da janela GTK não entram nesta conta: a janela não mudou e elas continuam sendo o registro datado do `interface.md`.

98f2a6a405fa4f2c064dd68074ed6e1f1b8e0075  06/09/2026  A PRIMEIRA DECLARAÇÃO DE UMA REMOÇÃO — sprint `GTK-3`, ONDA E (`D-0609-GTK-LEVA-INTEIRA`). Esta linha chega VERMELHA de herança: em `98f2a6a4` o portão já reprovava antes de eu editar um byte (medido numa worktree solta no próprio `98f2a6a4`: `1 failed, 6 passed`), porque a leva anterior mexeu em `src/hefesto_dualsense4unix/interface` e `…/app` sem refotografar. O retratista das DEZ (`src/hefesto_dualsense4unix/interface/olhar.py --todas --publicado --doc`) foi rodado nesta árvore JÁ com a janela GTK apagada — `gui/main.glade`, `app/app.py`, `app/main.py` e `scripts/gui-captura/` fora do disco — e as dez saíram BYTE A BYTE idênticas às commitadas: `git status --porcelain -- docs/usage/assets` devolveu zero linha, e o sha256 das dez bate (aba-01 `ce2a3260`, aba-02 `a2b54091`, aba-03 `3eae57df`, aba-04 `feebbc11`, aba-05 `a907e6aa`, aba-06 `0aa62c05`, aba-07 `a1e4cf25`, aba-08 `3461d507`, aba-09 `1d7e82b3`, aba-10 `61ddafcb`). É O QUE ESTA LEVA TINHA DE PROVAR: apagar a janela não move um pixel da tela que ela usa, porque a tela dela é HTML e o motor que a pinta ficou inteiro. As catorze `readme_*.png` da janela antiga NÃO entram nesta conta e não entram em nenhuma outra a partir de hoje — o estúdio que as tirava (`scripts/gui-captura/`) saiu junto com a janela, e `docs/usage/interface.md` passou a dizer isso onde antes prometia que elas acompanhavam a versão.

fb242e5afc246054f1b01e4954fd5e7c664c6f0b  08/09/2026  retratista das DEZ rodado nesta árvore (`interface/olhar.py --todas --publicado --doc`), já com a aba 02 republicada. As dez saíram BYTE A BYTE idênticas às commitadas — `git status --porcelain -- docs/usage/assets` devolveu zero linha para os dez `aba-NN-*.png`. É o desfecho ESPERADO e ele é a prova do que esta leva fez: o que mudou na aba 02 foram DOIS atributos `title` (o «Virtual» e o «Nativo») e uma frase dentro do `?` da linha do microfone, que passou de «à direita» para «logo abaixo» — e `title` não se pinta numa foto. Quem mede tooltip é o DOM, não o retratista: medido no WebKit do produto, com `Gtk.OffscreenWindow`, os quatro cartões trazem o texto novo, o 🎙 está em y=358 e o «Virtual» em y=385 (o «logo acima» do texto é medido, não suposto), o clique chega ao despachante e a frase velha aparece zero vez. DUAS fotos que NÃO entram, e não são desta leva: `aba-04-iluminacao.png` e `aba-07-lancadores.png` saíram diferentes das commitadas nesta árvore, e nada em `interface/paginas/04-*` nem `07-*` foi tocado aqui — foram devolvidas com `git checkout --`. Commitá-las trocaria o registro dela pelo desta máquina, calada, que é exatamente o que esta régua avisa.

23cb20c093658774a16a92350ba351bdfa60bcce  13/09/2026  MODO-DE-CONEXAO-01, na validação. O retratista das DEZ (`src/hefesto_dualsense4unix/interface/olhar.py --todas --publicado --doc`, e de novo com `--vista dela`) foi rodado nesta árvore, e as vinte fotos saíram diferentes das commitadas — inclusive as das nove abas cujas páginas esta sprint não tocou (02 a 10), o que é o render desta máquina e não a tela: foram devolvidas com `git checkout --`. A prova da 01 é outra: a página publicada de `e2820f17` e a desta árvore, fotografadas em seguida no mesmo Chrome headless, saíram BYTE A BYTE idênticas (md5 `f8d9ef2a`). O que mudou na 01 foram os `title` dos quatro chips de modo, o texto do «?» do quadro Modo e duas linhas da legenda, e nenhum deles aparece na página em repouso; o «?» aberto por hover foi fotografado à parte e mostra o texto novo. O commit declarado é o último de código de tela da validação, e ele só troca um comentário de `app/draft_config.py`.

1a6cc7c86514469335d1b3704bd31ba4e5278ed5  13/09/2026  costura da onda 5 da terceira lista. O commit troca só a expectativa das `PROVAS` dos chips de modo em `src/hefesto_dualsense4unix/interface/pacotes/a01_jogar.py` (o comentário e os dois valores esperados passam de `flavor` para `caminho`), que não se pinta: a aba 01 regerada e publicada saiu sem diferença. O retratista das DEZ (`src/hefesto_dualsense4unix/interface/olhar.py --todas --publicado --doc`, e de novo com `--vista dela`) rodou nesta árvore logo depois das fotos de `368368c7` e `001eaeb5`, e as vinte saíram BYTE A BYTE idênticas: `git status` mostrou só os dois recibos `PROVA-DA-FOTO.txt`, devolvidos com `git checkout --`.

d7c28bc840be7045c22d6ecfa9322bdddc4015d9  14/09/2026  costura da onda 6 da terceira lista. O commit troca só a docstring de `src/hefesto_dualsense4unix/app/ipc_bridge.py` que chamava o `input:iec958-stereo` de «S/PDIF sem sinal» (o fato velho que a MIC-PADRAO-NO-CABO-01 derrubou), e docstring não se pinta. O retratista das DEZ (`src/hefesto_dualsense4unix/interface/olhar.py --todas --publicado --doc`, e de novo com `--vista dela`) rodou nesta árvore sobre `d7c28bc8`, e as vinte fotos saíram BYTE A BYTE idênticas: `git status` mostrou só os dois recibos `PROVA-DA-FOTO.txt`, devolvidos com `git checkout --`.

6ed6df8d76a84b3276c3bd0a1f7e952ea7891cba  15/09/2026  A-TELA-QUE-TRAVA-01, a leitura do estado sai do laço do GTK. O commit não toca desenho nenhum: `git show --name-only` dá cinco arquivos, e nenhum é página publicada, gerador de aba ou mockup. As três mudanças em `interface/pacotes/` são SÓ reapontamento de citação de linha (`hefesto_vivo.py:3249` -> `:3371`, `:4441` -> `:4599`, `:3222` -> `:3344`), prosa de comentário que não se pinta; o resto é `hefesto_vivo.py` (o `LeitorDoEstado`, o fio que lê fora do laço) e a régua nova. O retratista das DEZ (`src/hefesto_dualsense4unix/interface/olhar.py --todas --publicado --doc`, e de novo com `--vista dela`) rodou nesta árvore sobre este commit, e CINCO das vinte fotos saíram com bytes diferentes — deltas de 30 a 907 bytes em `aba-08`, `aba-09`, `aba-10`, `maximizada/aba-01` e `maximizada/aba-02`. É o estado VIVO desta máquina (bateria, giroscópio, acelerômetro, os números que andam sozinhos), e não a tela: commitá-las trocaria a mesa dela pela desta hora, calada. Devolvidas com `git checkout -- docs/usage/assets/`.

dcada2d59a045a50b49f27c730f7d54d0d4f93e4  16/09/2026  a subida do pino para o GE-Proton 11-6. O commit toca a tela em UM lugar — `interface/aba09.py` e a página `09-sistema.html` que ela gera —, e é só a frase de apoio do item «Refazer a fixação do Proton», que ganhou «— o que faz o controle vibrar e tocar som dentro do jogo». A frase É visível (não é tooltip), mas cai abaixo da dobra da vista fotografada: as duas vistas cortam a aba Sistema com «209 px mortos embaixo». O retratista das DEZ (`src/hefesto_dualsense4unix/interface/olhar.py --todas --publicado --doc`, e de novo com `--vista dela`) rodou nesta árvore, e as VINTE fotos saíram byte a byte idênticas: `git status` mostrou só os dois recibos `PROVA-DA-FOTO.txt`, devolvidos com `git checkout --`. Nada a repintar — e fica a medição para a próxima pessoa: mudança no rodapé da aba 09 não alcança a foto, então ela precisa ser conferida pelo HTML, não pela imagem.

79981a25672de9e5751c51d331a73781ce75632a  16/09/2026  a leva de 16/09 (o perfil manda · o pino · o alto-falante roteado · o replug · as quatro pernas · o Vulkan que refaz). Cinco arquivos de `interface/` aparecem no diff — `aba10.py`, `pacotes/a02_controles.py`, `pacotes/a06_navegacao.py`, `pacotes/a08_conexoes.py` e `pacotes/a09_sistema.py` —, e nos CINCO a mudança é SÓ reapontamento de citação de linha em comentário: as inserções no `backend_pydualsense.py` (a constante `ROTA_PADRAO_DO_SOM` e o docstring da SOM-ROTA-02) empurraram as linhas citadas, e o reaponte foi por símbolo, com o git como oráculo. Comentário não se pinta. O retratista das DEZ (`src/hefesto_dualsense4unix/interface/olhar.py --todas --publicado --doc`, e de novo com `--vista dela`) rodou nesta árvore sobre este commit, e as VINTE fotos saíram byte a byte idênticas: `git status` mostrou só os dois recibos `PROVA-DA-FOTO.txt`, devolvidos com `git checkout --`.

329f5995bdef483aface2f84ccd3bde7eb200f99  16/09/2026  as duas curas do microfone (o mudo que sobrevive ao replug · o produto que parava de mandar desfazer a escolha dela). UM arquivo de `interface/` aparece no diff — `pacotes/a08_conexoes.py` —, e a mudança é SÓ reapontamento de citação de linha em comentário: a inserção de `reapply_mic_on_connect` no `profiles/manager.py` empurrou o bloco de `_controllers_to_rumble_scales`, e as duas citações foram reapontadas POR SÍMBOLO (`:2577` -> `:2691`, ancorada em `if cfg.rumble is None:`; `:2534-2579` -> `:2664-2714`, o bloco inteiro da função). Comentário não se pinta. O retratista das DEZ (`src/hefesto_dualsense4unix/interface/olhar.py --todas --publicado --doc`, e de novo com `--vista dela`) rodou nesta árvore sobre este commit, e as VINTE fotos saíram byte a byte idênticas — conferido por `md5sum` das 41 imagens, não só por `diff`. `git status` mostrou só os dois recibos `PROVA-DA-FOTO.txt`, devolvidos da cópia guardada antes da corrida. ARMADILHA MEDIDA AQUI, e é nova: o `olhar.py` chamado como EXECUTÁVEL usa o shebang e cai no python do sistema, que não tem `playwright` — `ModuleNotFoundError` com rc=1, e um `rc` ignorado se leria como "fotografei e não mudou". Chame-o com `.venv/bin/python`.

5431b481c57c8d0136c82afeab5276ffc21b4bec  18/09/2026  a leva INSTALL-UNIVERSAL (as treze lacunas que só valiam na máquina dela: háptica, doctor, Proton pinado, ambiente da Steam, microfone). Três arquivos de `interface/` aparecem no diff, e `interface/paginas/` saiu intacta: `aba06.py` e `pacotes/a06_navegacao.py` mudam SÓ citação de linha em comentário (o `lifecycle.py` cresceu doze linhas, reapontadas por símbolo); `pacotes/a09_sistema.py` muda o COMPORTAMENTO do «Refazer a fixação do Proton» — o clique 1 passa a perguntar se o Proton pinado está no disco e, sem ele, recusa com a frase que já existia —, que só aparece depois de um clique e não mora na página. O retratista das DEZ (`src/hefesto_dualsense4unix/interface/olhar.py --todas --publicado --doc`, e de novo com `--vista dela`, pelo `.venv/bin/python`) rodou na árvore de integração sobre este commit: as vinte fotos saíram iguais A OLHO, e duas (`aba-03-gatilhos.png`, `aba-05-vibracao.png`) com bytes diferentes — lidas lado a lado, a diferença é o antialias da fileira de botões do rodapé («Aplicar», «Salvar Perfil»), ruído de renderização e não desenho. Não commitei os bytes novos: as quatro mudanças foram devolvidas com `git checkout --`.

d0920e630ef11d108b1cba8f59ed918c7cd02afd  20/09/2026  AS-FOTOS-ALCANCAM-A-TELA-01. As vinte fotos estavam em `52ad313c9` e UM só commit de `CODIGO_DA_TELA` veio depois dele — este —, com UM só arquivo de tela no diff: `interface/paginas/06-navegacao.html`, uma linha DENTRO de um comentário HTML, que reaponta a citação `hefesto_vivo.py:1257` para `:1329`. Comentário não se pinta, e o retratista confirmou em vez de deduzir: as DUAS famílias foram refeitas nesta árvore pelo `.venv/bin/python` (`interface/olhar.py --todas --publicado --doc`, rc=0, e de novo com `--vista dela`, rc=0), e as vinte fotos saíram BYTE A BYTE idênticas — sha256 das dez de `docs/usage/assets` e das dez de `maximizada/` tirado ANTES da corrida e comparado depois, `diff` vazio. `git status --short -- docs/usage/assets/` devolveu só os dois recibos `PROVA-DA-FOTO.txt`, e neles a única linha alterada é o carimbo `ensaio:` (02:02 para 02:49); devolvidos com `git checkout --`, pelo mesmo motivo de sempre — não refotografar só para gerar bytes. Nenhuma imagem entra neste commit: o gesto que o portão cobra não produziu byte nenhum, e é a segunda porta que o registra.

757b390604c438727626f718143025f8c060deac  20/09/2026  a costura das duas frentes de 20/09 (as SETE FRASES que cravavam o instalador · o DADO que saiu de `docs/process` para `docs/method`) mais as sete curas da suíte de 24 partes. NENHUM arquivo de `interface/paginas/` aparece no diff, e os dois de `interface/pacotes/` que aparecem (`a02_controles.py`, `a09_sistema.py`) mudam FRASE DE RECADO — o texto que o produto imprime quando falta algo —, não desenho: o que muda é o gesto que a frase manda fazer (`./install.sh` deixa de ser cravado em cinco dos seis formatos que não o têm). Frase de recado não mora na página; ela nasce no clique. O retratista rodou as DUAS famílias nesta árvore pelo `.venv/bin/python` (`interface/olhar.py --todas --publicado --doc`, rc=0, e de novo com `--vista dela`, rc=0) e as TRINTA E SEIS fotos saíram byte a byte idênticas — sha256 de todas tirado ANTES da primeira corrida e comparado depois das duas, `diff` vazio. `git status --short -- docs/usage/assets/` devolveu só os dois recibos `PROVA-DA-FOTO.txt`, com o carimbo `ensaio:` como única linha alterada; devolvidos com `git checkout --`, pelo motivo de sempre: a foto carrega o estado VIVO da mesa de quem a tira, e commitar bytes novos troca a mesa dela pela minha, calada. Nenhuma imagem entra neste commit.

603c9b1f50a51468085a79a1966215cbeb762314  20/09/2026  a leva de três (BARRA-MAXIMIZADA-01 · CONEXAO-ZUMBI-01 · O-BOTAO-ENTREGA-O-QUE-PROMETE-01) mais o par do mapa. O portão disparou porque `interface/aba02.py` conta como CÓDIGO DA TELA e cresceu 166 linhas — e ele está CERTO em disparar: gerador mudado é motivo legítimo para perguntar se a tela mudou. A resposta, medida antes de fotografar: `git diff --name-only 2010645..603c9b1f5 -- src/hefesto_dualsense4unix/interface/paginas/` devolve ZERO arquivos. A página publicada não foi tocada, porque o construtor do botão foi instruído a NÃO publicar a aba 02 — a aprovação dela foi sobre a foto da BANCADA, e publicar é ato de quem coordena, com a palavra dela. Os outros seis arquivos de `interface/` no diff mudam GERADOR e PACOTE (aba02.py, a02_controles.py, a01_jogar.py, a06_navegacao.py, a08_conexoes.py, a09_sistema.py): o que eles emitem vai para `mockup/`, e o que o produto renderiza sai de `interface/paginas/`, intacta. O retratista rodou as DUAS famílias nesta árvore pelo `.venv/bin/python` e as TRINTA E SEIS fotos saíram byte a byte idênticas — sha256 de todas tirado ANTES e comparado depois das duas corridas, `diff` vazio. `git status --short -- docs/usage/assets/` devolveu só os dois recibos `PROVA-DA-FOTO.txt`, com o carimbo `ensaio:` como única linha alterada; devolvidos com `git checkout --`. Nenhuma imagem entra neste commit.

7c00fea61f440a51ce5e3d9e01ca36cac385573c  23/09/2026  a conferência da TRANSPLANTE-DA-SECAO-01 (9e6f746a8, 57397ad24, 7c00fea61). O diff de tela é o `aba08.py` em comentário e o `a08_conexoes.py` no que o piloto pinta ANTES de o BlueZ e o `maquina.json` responderem — nenhum arquivo de `interface/paginas/` nem de `mockup/`. As duas famílias foram refeitas nesta árvore (`olhar.py --todas --publicado --doc`, rc=0, e com `--vista dela`, rc=0), com o sha256 das 36 tirado antes e comparado depois: 35 idênticas, e a `maximizada/aba-07-lancadores.png` difere em 833 pixels espalhados pelo contorno das letras — ruído de antisserrilhado, numa aba que ninguém tocou. Devolvidas com `git checkout --`; nenhuma imagem entra.
4e3670fcac150dca366d2b4b4ea9520fc3b5e02f  23/09/2026  o fecho da leva do rádio na integração (a 4b: INSTALL-E-UNINSTALL-DO-RADIO-01 e A-CALIBRACAO, mais «mesa» fora do desenho do rádio em 3f6c12273 e 4e3670fca). As duas famílias refeitas (`olhar.py --todas --publicado --doc`, rc=0, e com `--vista dela`, rc=0): 35 idênticas, e a `maximizada/aba-07-lancadores.png` difere só no antisserrilhado, como na conferência da TRANSPLANTE. A Calibrar mudou só na bancada (mockup), sem publicar. Devolvidas com `git checkout --`; nenhuma imagem entra.
eb646e392b24a10955841149b8c67315277518ac  23/09/2026  o ▶ do adaptador com o glifo em aria-hidden (eb646e392): nenhuma mudança visível. As duas famílias refeitas (rc=0 e rc=0): 35 idênticas, e a `maximizada/aba-07-lancadores.png` difere só no antisserrilhado, como nas duas conferências anteriores. Devolvidas com `git checkout --`; nenhuma imagem entra.
d4eaa2081bee1e80516b422da793826a8fd69c31  24/09/2026  a vista dela (--vista dela --doc) refeita depois da publicação da Calibrar e do «ATIVO» e da A-MIRA: as fotos da maximizada saíram idênticas
295c36a25a21c5053ab6e168149fa8d47e9896e0  24/09/2026  o lugar vazio do alto-falante (295c36a25): nenhuma mudança visível nas fotos. As duas famílias refeitas (rc=0 e rc=0): a vista dela idêntica, e a aba-03-gatilhos.png difere em 608 pixels de antisserrilhado, a mesma oscilação medida em 9e39147. Devolvidas com `git checkout --`; nenhuma imagem entra.
24c06a6cae8a013ee966759c4de8cca52e142453  24/09/2026  a STEAM-INPUT-01 (c4cbc243c, 425d75779, ca5d57660, 24c06a6ca): o chip «Steam Input» e a frase do «Detectar» mudam só o que o Python pinta — nenhum arquivo de `interface/paginas/` nem de `mockup/`. As duas famílias refeitas (`olhar.py --todas --publicado --doc`, rc=0, e com `--vista dela`, rc=0): 39 de 41 idênticas; a `aba-05-vibracao.png` difere em 674 pixels e a `aba-09-sistema.png` em 1542, espalhados pelo contorno das letras — antisserrilhado, conferido na imagem da diferença. Devolvidas com `git checkout --`; nenhuma imagem entra.
fd5c17b7041428ef900e7978f2c21bf8b7b08e88  24/09/2026  a STEAM-INPUT-01, o último commit de tela (fd5c17b70, o segundo clique que não fecha a Steam por nada): as duas famílias refeitas de novo (rc=0 e rc=0), com o mesmo resultado da linha de 24c06a6ca — 39 de 41 idênticas, e a `aba-05-vibracao.png` (674 pixels) e a `aba-09-sistema.png` (1542) só no antisserrilhado, os mesmos números. Devolvidas com `git checkout --`; nenhuma imagem entra.
715d265a13a8c6184e7c612c9a94a476defb411d  24/09/2026  a conferência da STEAM-INPUT-01 (30b7ce6a9, 715d265a1): a frase do «Detectar» e a frase do desligar do chip mudam só texto que o Python pinta (a do chip vai ao diário). As duas famílias refeitas (rc=0 e rc=0): as mesmas 39 de 41 idênticas, e a `aba-05-vibracao.png` (674 pixels) e a `aba-09-sistema.png` (1542) só no antisserrilhado, byte a byte iguais às da frente. Devolvidas com `git checkout --`; nenhuma imagem entra.
68c8c3a8194eb55893f59ce723038f503b67906d  24/09/2026  a AS-FRASES-QUE-A-BANCADA-ACHOU-01 (afe991e4c, 57b637821, 931d35a53, 68c8c3a81): a ajuda do «Testar agora» (05) e a palavra do transporte na Gestão de Controles e no Microfone (08) vão só ao `mockup/` e ao texto que o Python pinta — nenhum arquivo de `interface/paginas/` mudou. As duas famílias refeitas (`olhar.py --todas --publicado --doc`, rc=0, e com `--vista dela`, rc=0): só quatro abas mudaram nas duas famílias; a `aba-01-jogar.png` difere em 58 pixels, a `aba-04-iluminacao.png` em 622 (1184 na maximizada), a `aba-05-vibracao.png` em 250 e a `aba-06-navegacao.png` em 246, todas no contorno das letras do desenho do controle (L, R, L1, R1) — antisserrilhado, conferido no recorte lado a lado. Devolvidas com `git checkout --`; nenhuma imagem entra.
1e2488a1e8d943ffd973d329e8b68768c66b0935  24/09/2026  a conferência da AS-FRASES-QUE-A-BANCADA-ACHOU-01 (1e2488a1e): três comentários do `aba08.py` e do `a08_conexoes.py` que citavam a contagem velha — nenhum byte de `interface/paginas/` nem de `mockup/` (o gerador regerado deu as mesmas 05 e 08). As duas famílias refeitas (`olhar.py --todas --publicado --doc`, rc=0, e com `--vista dela`, rc=0): as mesmas quatro abas da linha de 68c8c3a81, com os mesmos números — `aba-01-jogar.png` 58 pixels, `aba-04-iluminacao.png` 622 (1184 na maximizada), `aba-05-vibracao.png` 250 e `aba-06-navegacao.png` 246 —, todos no contorno das letras L, R, L1 e R1 do desenho do controle (antisserrilhado, conferido na imagem da diferença). Devolvidas com `git checkout --`; nenhuma imagem entra.
e17ea02ccca791fe474ff48daa1bc49bf1634fa7  24/09/2026  a O-MODO-FREESTYLE-02 (c45f43303, 313579d29, 504b8bfe8, e17ea02cc, na branch voo/O-MODO-FREESTYLE-02): o perfil «Freestyle», o Salvar sem perfil ativo, a dica «Desligue» e a faixa do Steam Input sem o vigia mudam o que o Python pinta e o desenho (mockup/01-jogar.html) — nenhum arquivo de `interface/paginas/`. As duas famílias refeitas (`olhar.py --todas --publicado --doc`, rc=0, e com `--vista dela`, rc=0): 16 de 20 idênticas em cada; aba-01 (58 pixels), aba-04 (622; 1184 na vista dela), aba-05 (250) e aba-06 (246) só no antisserrilhado, conferido na imagem da diferença. Devolvidas com `git checkout --`; nenhuma imagem entra. Depois do cherry-pick o SHA muda, e a conferência se refaz na integração.
98015e1045b53230c06436bdac671e1f4d98c260  24/09/2026  a conferência da O-FLATPAK-ALCANCA-O-BLUEZ-01 (98015e104): só dois comentários do `secao_mesa.py` mudam, nenhum texto de tela. As dez abas refotografadas na integração da 6c: 04 e 07 com 562 e 833 pixels de antisserrilhado espalhados pelas letras, nenhuma entra.
e967cfd133b6f2ae0e765747be658c70ab2f48ff  24/09/2026  a STORM-USB-02 (e967cfd13): o `app/fatos_do_mapa.py` regerado com a linha nova `plataforma.religar@dualsense` — chave que nenhuma aba lê, nenhum arquivo de `interface/paginas/` nem de `mockup/`. As duas famílias refeitas num lar de mentira (`olhar.py --todas --publicado --doc`, rc=0, e com `--vista dela`, rc=0): 16 de 20 idênticas em cada; aba-01 (58 pixels), aba-04 (622), aba-05 (250) e aba-06 (246) só no antisserrilhado das letras L, R, L1 e R1 do desenho do controle, conferido na imagem da diferença. Devolvidas com `git checkout --`; nenhuma imagem entra.
07fe24c851cc466a50b8ce0f72f7a711721bc4b4  24/09/2026  a costura da onda 6e-1 na integração (O-VPAD, O-MODO-FREESTYLE-03, STORM-USB-02 e A-HAPTICA, com o e967cfd13 da STORM renomeado pelo cherry-pick) e a publicação das dez (07fe24c85), que muda só o `title` congelado do «Salvar Perfil», invisível na foto. As duas famílias refeitas num lar de mentira (`olhar.py --todas --publicado --doc`, rc=0, e com `--vista dela`, rc=0): 16 de 20 idênticas em cada; 01, 04, 05 e 06 com 58 a 622 pixels de antisserrilhado no brilho das luzes e nas letras, nenhuma entra.
2cf621731dfd0089ca3e408bc62b2ac2744cd8aa  25/09/2026  a A-MASCARA-SEGUE-O-ESTADO-01 (2cf621731): o juiz das máscaras mora no daemon, e o que toca a interface são dez citações `arquivo:linha` reapontadas em comentários (`aba06.py` e `a06_navegacao.py`) — nenhum byte de `interface/paginas/` nem de `mockup/`. As duas famílias refeitas num lar de mentira (`olhar.py --todas --publicado --doc`, rc=0, e com `--vista dela`, rc=0): a família da documentação idêntica; na maximizada, a `aba-01-jogar.png` difere em 1059 pixels e a `aba-10-perfis.png` em 1044, espalhados pela tela (17 e 43 com tolerância de 15%) — antisserrilhado. Devolvidas com `git checkout --`; nenhuma imagem entra.
